import { createApp } from 'vue'
import App from './App.vue'

createApp(App).mount('#app')
// import axios from 'axios';

// axios.defaults.baseURL = 'http://localhost:8000'; // Cambia la URL según tu configuración

// Vue.prototype.$axios = axios;
